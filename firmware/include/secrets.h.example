#pragma once

// Copy this file to secrets.h (in this same directory) and fill in real
// values. secrets.h is gitignored - never commit it.
//
// Never log these values, and never publish them to any MQTT topic.

#define WIFI_SSID     "REPLACE_ME"
#define WIFI_PASSWORD "REPLACE_ME"

// Broker connection details live here, not in config.h: they're
// deployment-specific (this Pi's LAN address) rather than a fixed project
// constant, and sit naturally next to the credentials they're paired with.
#define MQTT_BROKER_HOST "REPLACE_ME" // e.g. 192.168.x.x, the Pi's LAN address
#define MQTT_BROKER_PORT 1883
#define MQTT_USERNAME    "REPLACE_ME"
#define MQTT_PASSWORD    "REPLACE_ME"

#define OTA_PASSWORD "REPLACE_ME"
